#!/bin/bash
#for x in `ls ${GITREPODIR}/promotion/release_steps/crons/${HOSTPREFIX}*/*.crontab`; do
#    FILENAME=`basename $x`
#    USERNAME=${FILENAME%%.*}
#    DIRNAME=`dirname $x`
#    TARGETHOST=`basename ${DIRNAME}`
#done
